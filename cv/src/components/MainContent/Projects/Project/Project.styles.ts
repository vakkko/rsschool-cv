import type { SxProps, Theme } from "@mui/material";

export const projectBoxStyles: SxProps<Theme> = {
  display: "flex",
  flexDirection: "column",
  mb: "1rem",
};

export const linkStyles: SxProps<Theme> = {
  display: "flex",
  alignItems: "center",
  gap: "0.3rem",
};
